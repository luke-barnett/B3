<Window x:Class="IndiaTango.Views.SessionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        xmlns:caliburn="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
        Title="{Binding ApplicationTitle}" Icon="{Binding Icon}" Height="450" Width="450">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ListBox Grid.Column="0"  ItemsSource="{Binding SensorList}" SelectedItem="{Binding ItemList1Selected, Mode=TwoWay}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <caliburn:ActionMessage MethodName="btnGraph" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </ListBox>
        <Grid Grid.Column="1" Margin="10 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="260" />
                <RowDefinition Height="30" />
                <RowDefinition Height="10" />
                <RowDefinition Height="30" />
                <RowDefinition Height="10" />
                <RowDefinition Height="15" />
                <RowDefinition Height="10" />
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>
            <Button Grid.Row="1" Name="btnImport" IsEnabled="{Binding ButtonsEnabled}" Content="Import" />
            <Button Grid.Row="3" Name="btnGraph" IsEnabled="{Binding ButtonsEnabled}" Content="Graph" />
            <ProgressBar Grid.Row="5" IsIndeterminate="False" Visibility="{Binding LoadingComponentsVisible}" Value="{Binding ProgressBarPercent}"/>
            <Button Grid.Row="7" Name="btnCancel" Visibility="{Binding LoadingComponentsVisible}" Content="Cancel" />
        </Grid>
    </Grid>
</Window>
